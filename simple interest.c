#include<stdio.h>
int main()
{
int p,t,r,SI;
    printf("Enter the value of p ,t and r");
    scanf("%d%d%d",&p,&t,&r);
    SI=(p*t*r)/100;
    printf("The SI is %d",SI);
}
Enter the value of p ,t and r2 3 5
The SI is 0

=== Code Execution Successful ===
